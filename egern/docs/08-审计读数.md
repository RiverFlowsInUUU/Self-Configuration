# 08 · 审计读数

> 本仓库自带审计脚本，可直接对模板运行。脚本在 [`skill/scripts/egern/`](../../skill/scripts/egern/)，
> 命令清单与逐条判据见 [`skill/reference/egern/checker.md`](../../skill/reference/egern/checker.md)。
>
> ⚠️ **全部验证都在本地完成** —— 本仓库**刻意不挂 CI / 任何自动化**（理由见 [`skill/README.md`](../../README.md)）。

---

## 对推荐版 `routing_v3.2` 的读数

| 脚本 | 读数 |
|:----:|:----:|
| `check_egern_dns.py` | ✅ **0 high**（退出码 0），另有 2 条 `LOW` |
| `audit_ruleset_noresolve.py` | ✅ 全部通过（`routing_v3` / `routing_v3.1` / `routing_v3.2` 各 25 个规则集 · `routing_v1`~`routing_v2.4` 各 21 个 · `lazy` **11 个** = `rules` 8 条 + `dns.forward` 3 条。`routing_v3.2` 与 `v3.1` 同数但组成不同：新增 `apple_system.list`、`Proxy.list` 改纯注释后不再被引用） |
| `audit_routing_coverage.py` | ✅ 15/15 国内探针命中 `DIRECT` |
| `audit_dns_forward.py` | ✅ 通过（带 / 不带 `--drill` 都通过） |
| `audit_region_filters.py` | ✅ 6 个地区组的关键词全部同步进 `Other Regions` 的负向断言（`lazy` 无此结构，自动跳过） |
| `audit_ruleset_refresh.py` | ✅ `--strict` 名单两条：`routing_v3.2` **22 条** · `lazy` **8 条**远程规则集，全部 `update_interval: 604800`（退出码 0） |
| `skill/tests/check_doc_readings.py` | ✅ 懒人版 3 组 / 10 条规则 / 8 条规则集；分流版 26 组 / 24 条规则 / 22 条规则集 |

> 🆕 `check_doc_readings.py` 与 Surge 侧同一份（全仓共用、2026-09-24 起是 `all.sh` 第 5 项、已在冻结闸门名单内）：
> 它不判配置对错，只把文档里写死的读数与实测值对拍，固定 18 条（D0–D18；D14 无此条，是历史扩表时编错的号）。🆕 2026-09-25 起新增 D18：两内核分流版规则集的 URL **交集**（含已注释项）== 文档写死的共用份数，现算 21。

> `check_egern_dns.py` 的读数会随版本略有差异（`ok` 条数不同）—— 逐版读数见
> [`docs/07-文件版本沿革.md`](07-文件版本沿革.md)。上表是推荐版 `routing_v3.2` 的读数（`v3.2` 相对 `v3.1` 动了位 ⑲ 与
> `Proxy.list` 的写法，组数 / 规则数 / 静态读数不变）。

---

## 那 2 条 `LOW` 是什么

**不是缺陷，是刻意的设计取舍**：

1. **设置了 `proxy_nameservers`** —— 它成为代理侧解析的唯一出口；
2. **兜底指向国内组** —— 需要本地解析的境外域名会拿到国内答案，实际影响面仅限 `DIRECT` 域名。

完整取舍说明见 [`DetailsReadme` §6](../DetailsReadme/DetailsReadme.md#6-已知代价与取舍)。

---

## 回归测试

```bash
bash skill/tests/egern/run.sh
```

两阶段共 **24 个断言**，退出码非 0 即失败（**联网口径**；`SKIP_NET=1` 时那条联网项跳过 ⇒ 22）：

- **阶段 1** —— 5 个 fixture × 2 个脚本 = **10**
- **阶段 2** —— 顶层固定名四件 ×（DNS 面 + 地区 filter 同步 + 规则集刷新参数）= **12**
  （归档版在 `config_old/`，不在通配里）；另**完整版两份** × 1 条联网项（规则集内容 / 分流覆盖）= **2**，
  `.min` 刻意跳过（同一份配置去注释），`SKIP_NET=1` 时这 2 条也跳过
